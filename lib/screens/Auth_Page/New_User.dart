import 'package:flutter/material.dart';

class new_user_screen extends StatelessWidget {
  const new_user_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("New User"),
    );
  }
}
