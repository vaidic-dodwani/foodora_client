import 'package:flutter/material.dart';

class signout_screen extends StatelessWidget {
  const signout_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Sign Out Screen")),
    );
  }
}
